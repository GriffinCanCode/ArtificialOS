import { defineNativeAppConfig } from '../vite.config.base';

/**
 * Vite configuration for browser
 * Extends the shared base configuration
 */
export default defineNativeAppConfig('Browser', {
  // Add app-specific overrides here
});

