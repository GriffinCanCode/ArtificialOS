import { defineNativeAppConfig } from '../vite.config.base';

/**
 * Vite configuration for Hub App
 * Extends the shared base configuration
 */
export default defineNativeAppConfig('Hub', {
  // Add app-specific overrides here if needed
});

